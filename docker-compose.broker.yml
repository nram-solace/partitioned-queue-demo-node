# Deprecated: in-network SOLACE_HOST / SEMP overrides live in docker-compose.yml.
# Use: docker compose up -d --build
# (Keeping this file avoids breaking bookmarks that pass -f docker-compose.broker.yml)
