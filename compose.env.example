# Copy to .env in the repo root so `docker compose` enables the bundled broker profile:
#   cp compose.env.example .env
#
# SOLACE_HOST for Node containers is set in docker-compose.yml (ws://solace-broker:8008).
# Browser URL comes from SOLACE_PUBLIC_URL in demo.env.

COMPOSE_PROFILES=broker
